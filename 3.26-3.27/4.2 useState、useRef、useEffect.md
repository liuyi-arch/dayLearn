# useState
### useState是同步还是异步的，为什么?

> Q1：React 18 的自动批处理（Automatic Batching）相比之前版本有什么改进？
>
> A1：
>
> Q2：在 React 18 的并发模式下，useState 的更新逻辑有什么变化？如何处理多个优先级的更新？ (考察并发渲染与优先级调度)
>
> A2：
>
> 

### useState为什么只能在React函数组件的顶层作用使用？

> Q1：既然 Hooks 依赖顺序，那 React 官方是如何强制开发者遵守这一规则的？（提示：eslint-plugin-react-hooks）
>
> A1：
>
> Q2：为什么 Hooks 不能写在 if 语句或循环中？如果打破了这个规则，React 会报错吗？报错信息是什么？
>
> A2：
>
> Q3：为什么 React 不使用 Key（类似组件的 key 属性）来标识 Hooks，从而允许在条件语句中使用？（提示：性能开销、API 简洁性、心智负担的权衡）
>
> A3：

### useState是如何保证状态更新时基于上个状态的？





# useRef

# useEffect


